node {
    def rootDir = PATH
    def exampleModule = load "${rootDir}/pipeline.Groovy "
    exampleModule.greetMsg()
}
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
              echo 'Building..'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
              //  exampleModule.greetMsg() 
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
